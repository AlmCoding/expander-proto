syntax = "proto3";
option optimize_for = LITE_RUNTIME;

package uart_proto;

enum UartId {
  UART1 = 0;
  UART2 = 1;
}

message UartConfig {
  UartId uart_id = 1;
  uint32 baud_rate = 2;
}

message UartData {
  UartId uart_id = 1;
  bytes data = 3;
}
